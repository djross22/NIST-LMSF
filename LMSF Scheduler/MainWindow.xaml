<Window x:Class="LMSF_Scheduler.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LMSF_Scheduler"
        mc:Ignorable="d"
        Title="LMSF Scheduler" Height="450" Width="800" Background="{DynamicResource PurpleBackgroundBrush}">
    <Grid>
        <DockPanel Margin="0">
            <Menu x:Name="___No_Name_" VerticalAlignment="Top" DockPanel.Dock="Top" FocusManager.IsFocusScope="False">
                <MenuItem Header="_File">
                    <MenuItem x:Name="newMenuItem" Header="New" Click="NewMenuItem_Click"/>
                    <MenuItem x:Name="openMenuItme" Header="Open" Click="OpenMenuItme_Click"/>
                    <MenuItem x:Name="saveMenuItem" Header="Save" Click="SaveMenuItem_Click" />
                    <MenuItem x:Name="saveAsMenuItem" Header="Save As..." Click="SaveAsMenuItem_Click" />
                    <Separator />
                    <MenuItem x:Name="exitMenuItem" Header="Exit" Click="ExitMenuItem_Click" />
                </MenuItem>
            </Menu>
            <ToolBarTray VerticalAlignment="Top" DockPanel.Dock="Top">
                <ToolBar HorizontalAlignment="Left">
                    <Button x:Name="startButton" ToolTip="Start Experiment" VerticalAlignment="Top" Margin="5,0" BorderBrush="Black">
                        <Image Source="start.png" Height="25"/>
                    </Button>
                    <Button x:Name="pauseButton" ToolTip="Pause Experiment" VerticalAlignment="Top" Margin="5,0" BorderBrush="Black">
                        <Image Source="pause.png" Height="25"/>
                    </Button>
                    <Button x:Name="stepButton" ToolTip="Single Step" VerticalAlignment="Top" Margin="5,0" BorderBrush="Black">
                        <Image Source="step.png" Height="25"/>
                    </Button>
                    <Button x:Name="abortButton" ToolTip="Abort Experiment" VerticalAlignment="Top" Margin="5,0" BorderBrush="Black">
                        <Image Source="abort.png" Height="25"/>
                    </Button>
                </ToolBar>
            </ToolBarTray>
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Height="30">
                <Button x:Name="testButton" Content="Test" Width="75" Click="TestButton_Click" Margin="10,0,20,10"/>
                <TextBox x:Name="testTextBox" TextWrapping="Wrap" Text="TextBox" Margin="0,0,0,10" Width="523"/>
                <Button x:Name="testWriteButton" Content="Write" Width="75" Margin="10,0,0,10" Click="TestWriteButton_Click"/>
            </StackPanel>
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="10*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="inputTextBox" Margin="10,10,10,0" TextWrapping="Wrap" Text="{Binding InputText, Mode=TwoWay}" Grid.Row="1"/>
                <TextBlock Margin="10,5,10,0" TextWrapping="Wrap" Text="    Steps Input:" Background="White" FontWeight="Bold" VerticalAlignment="Bottom" Height="20"/>
                <TextBox x:Name="outputTextBox" Grid.Column="1" Margin="10,10,10,0" Grid.Row="1" TextWrapping="Wrap" Text="{Binding OutputText, Mode=TwoWay}"/>
                <TextBlock Grid.Column="1" Margin="10,5,10,0" TextWrapping="Wrap" Text="    Steps Output:" Background="White" FontWeight="Bold" VerticalAlignment="Bottom" Height="20"/>
            </Grid>
        </DockPanel>

    </Grid>
</Window>
